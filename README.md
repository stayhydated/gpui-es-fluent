# gpui-es-fluent

Shared GPUI integration helpers for `es-fluent`.

This crate owns the app-global `I18n` wrapper, typed language bounds, locale
selection helpers, and fallback rendering used by GPUI crates that embed
`es-fluent` resources.
