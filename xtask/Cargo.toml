[package]
name = "xtask"
version = "0.1.0"
rust-version = "1.96"
edition = "2024"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
anyhow = "1.0"
clap = { features = [ "derive" ], version = "4.6" }
stayhydated-xtask = { workspace = true }
web = { path = "../web" }
